{
  "name": "@cadaster/cadaster",
  "version": "0.0.0",
  "private": true,
  "description": "Geocoding and co",
  "scripts": {
    "lint": "standard packages/*/lib/**/*.js",
    "test": "lerna run test --concurrency 1 --stream --ignore @cadaster/cadaster-lab",
    "test:coverage": "lerna run test:coverage --concurrency 1 --stream",
    "postinstall": "lerna bootstrap --hoist"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cadaster/cadaster.git"
  },
  "keywords": [
    "cadaster"
  ],
  "author": "Miro Ter-Seri",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/cadaster/cadaster/issues"
  },
  "homepage": "https://github.com/cadaster/cadaster#readme",
  "devDependencies": {
    "@commitlint/cli": "^8.0.0",
    "@commitlint/config-conventional": "^8.0.0",
    "ava": "^2.0.0",
    "commitizen": "^3.1.1",
    "cz-conventional-changelog": "^2.1.0",
    "husky": "^2.4.0",
    "lerna": "^3.14.1",
    "lint-staged": "^8.2.0",
    "standard": "^12.0.1",
    "standard-version": "^6.0.1"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
